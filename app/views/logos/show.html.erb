<%=  render 'players/nav' %>
<div class="tab-panel current" id="add-player">
  <div class="panel-links"><%= link_to '返回LOGO列表', logos_path %> |  <%= link_to '编辑', edit_logo_path(@logo) %></div>
<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @logo.name %>
</p>

    <table class="table table-bordered">
      <thead>
        <tr>
          <th>200 * 200 </th>
          <th>100 * 100</th>
          <th>50 * 50</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= image_tag(@logo.uri) if @logo.uri? %></td>
          <td><%= image_tag(@logo.uri_url(:normal)) if @logo.uri? %></td>
          <th><%= image_tag(@logo.uri_url(:thumb)) if @logo.uri? %></th>
        </tr>
      </tbody>
    </table>
</div>
