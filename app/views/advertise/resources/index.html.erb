<%=  render 'advertise/nav' %>
<div class="tab-panel current" id="add-player">
  <div class="panel-header clearfix">
    <a type="button" class="btn btn-green" href="<%= new_advertise_resource_path  %> ">添加广告资源</a>
  </div>
  <table class="table table-hover panel-table">
    <thead>
      <tr>
        <th>名称</th>
        <th>user</th>
        <th>创建时间</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <% @advertise_resources.each do |advertise_resource|  %>
      <tr>
        <td><%= advertise_resource.name %></td>
        <td><%= advertise_resource.user && advertise_resource.user.uid %></td>
        <td><%=  advertise_resource.created_at %></td>
        <td>
          <%= link_to '预览', advertise_resource_path(advertise_resource) %>
          <%= link_to '修改', edit_advertise_resource_path(advertise_resource) %>
          <td><%= link_to '删除', advertise_resource, method: :delete, data: { confirm: '你确定要删除?' } %></td>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <div class="panel-footer">
    <%=  paginate @advertise_resources %>
  </div>
</div>

