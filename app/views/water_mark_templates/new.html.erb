<%= render 'user_videos/nav' %>
  <% if @water_mark_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@water_mark_template.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @water_mark_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<h1>添加水印模板</h1>

<%= render 'form' %>

<%= link_to 'Back', water_mark_templates_path %>
