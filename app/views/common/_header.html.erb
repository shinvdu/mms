    <div class="header">
      <div class="title">
  <a class="header-brand text-white" href="#">MMS</a>
  <span class="text-purple">www.mz.com</span>
      </div><!--
        --><div class="user-options text-gray">
<% if current_account %>
  <div class="user-name">
    <span class="glyphicon glyphicon-user" aria-hidden="true"></span>
    <span><%=  @current_user && @current_user.nicename %></span>
  </div>
    <%= raw link_to('<span class="glyphicon glyphicon-off" aria-hidden="true"></span>'.html_safe, destroy_account_session_path, :method => :delete) %> 
<% else %>
    <%= link_to('註冊', new_registration_path(:account)) %> 
    <%= link_to('登入', new_session_path(:account)) %>
<% end %>
      </div>
    </div>
